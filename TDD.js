/* --- TDD --- */

var config = {
	/*
		variable contenant toutes les valeurs configurables.
	*/
}

function init (canvas)
{
	/*
		étape : 1
		inputs : canvas
		outputs : callback contenant les data de chaque heightmaps + execute la fonction qui créer la scene de la map globale "create_globaleMap_scene()"
	*/
}

function create_globaleMap_scene (canvas)
{
	/*
		étape : 2
		inputs : canvas
		outputs : callback contenant les data de chaque heightmaps + execute la fonction qui créer la scene de la map globale "create_globaleMap_scene()"
	*/
}